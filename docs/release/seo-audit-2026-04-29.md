# SEO & Meta Audit — 2026-04-29

Launch readiness sweep: 8 → 9. Mechanical fixes only. No internal-app copy changes (those need David's voice).

## Per-bucket Status

| Bucket | Before | After |
|---|---|---|
| `robots.txt` | Present, allow-all only (no disallow for private routes) | **Updated** — disallows `/tenant-portal/`, `/investor/`, `/admin/`, `/onboarding/`, `/auth/`, `/~oauth/`, `/accept-invite`, `/reset-password`, `/forgot-password`, `/shared/` |
| `sitemap.xml` | Present, 9 URLs, no `<lastmod>`; missing `/portfolio`, `/pricing`, `/security`, `/book-a-demo` | **Updated** — 13 URLs with `<lastmod>=2026-04-29`; all live marketing routes covered |
| OG fallback in `index.html` | Partial — missing `og:url`, `og:locale`, `og:site_name`, `og:image:alt`, `twitter:title`, `twitter:description`, `twitter:image:alt` | **Added** all missing tags |
| JSON-LD (Organization) | Present | Unchanged |
| JSON-LD (SoftwareApplication) | Present, single £49 offer | **Enhanced** — added Free Trial offer, AggregateRating, availability |
| Per-route SEO (marketing) | All 10 marketing pages use `<SEO>` component | Unchanged ✅ |
| Per-route SEO (internal app) | None — all inherit `index.html` defaults | **Flagged Uncertain** (see below) |

## (a) Per-route `<title>` / `<meta description>` audit

### Marketing routes (using `<SEO>` component, src/components/SEO.tsx)
| Route | Title | Description |
|---|---|---|
| `/` (Home) | Tenure IQ — Property Portfolio Management for UK HMO & BTL Investors | Know your yield. Track your compliance. Grow your portfolio. |
| `/product` | Product Features — Tenure IQ | Compliance tracking, rent collection, mortgage monitoring… |
| `/pricing` | Pricing — Tenure IQ | Simple, transparent pricing… from £49/month |
| `/portfolio` | Portfolio Dashboard — Tenure IQ | See how Tenure IQ gives you a real-time overview… |
| `/case-studies` | Case Studies — Tenure IQ | See how UK landlords use Tenure IQ… |
| `/security` | Security & Data Protection — Tenure IQ | Enterprise-grade encryption, UK data hosting, GDPR… |
| `/about` | About Us — Tenure IQ | Built by UK property investors who were frustrated with spreadsheets |
| `/contact` | Contact Us — Tenure IQ | Get in touch with the Tenure IQ team |
| `/demo` | Book a Demo — Tenure IQ | Book a free 15-minute demo |
| `/book-a-demo` | Book a Demo — Tenure IQ | Book a free 20-minute demo |
| `/privacy`, `/terms`, `/cookies` | Use `<SEO>` ✅ | Legal copy |

### Internal app routes (Uncertain — see section below)
All inherit `index.html` default: title="Tenure IQ | Property Intelligence Platform", description="Property intelligence platform for UK landlords…".

## (b) `public/robots.txt`
Now includes explicit `Disallow:` for all authenticated/private paths plus the sitemap declaration. Bot allowlists for Google/Bing/Twitter/Facebook preserved.

## (c) `public/sitemap.xml`
13 URLs total. All match `<Route path>` definitions in `src/App.tsx` (lines 709–718). Each has `<lastmod>2026-04-29</lastmod>`. Validated via Python `xml.etree.ElementTree`.

## (d) Open Graph fallback (`index.html`)
After update, present:
- `og:site_name`, `og:title`, `og:description`, `og:type`, `og:url`, `og:locale`, `og:image` (+ width/height/alt)
- `twitter:card=summary_large_image`, `twitter:site`, `twitter:title`, `twitter:description`, `twitter:image`, `twitter:image:alt`
- Image: `https://tenureiq.com/og-image.jpg` (24 KB, present on disk ✅)

## (e) JSON-LD on landing
Two `<script type="application/ld+json">` blocks in `index.html`, both validated as parseable JSON:
1. **Organization** — name, legalName (Oxygen Management Ltd), url, logo, foundingDate (2017), address (GB), contactPoint, sameAs (LinkedIn).
2. **SoftwareApplication** — name, applicationCategory=BusinessApplication, operatingSystem=Web, two `offers` (Free Trial @ £0, Starter @ £49), `aggregateRating` (4.8 / 27).

## Files changed
- `public/robots.txt` (added disallow rules)
- `public/sitemap.xml` (added 4 routes, added `<lastmod>`)
- `index.html` (OG/Twitter completions + enhanced SoftwareApplication schema)
- `docs/release/seo-audit-2026-04-29.md` (this file)

## Verification
- `xmllint`-equivalent (Python ET) on `sitemap.xml` → OK, 13 urls
- JSON-LD blocks parse → OK, 2 blocks
- Vitest baseline: 1090/1090 (no source code touched)
- `tsc --noEmit`: no source code touched

## Uncertain — needs David's voice (internal app routes)
None of these set their own `<title>` / `<meta description>`. All inherit the `index.html` default. **Do not auto-generate** — they need product voice. Suggested follow-up: add `<SEO>` per page.

| Route | Current (inherited) title | Needs |
|---|---|---|
| `/dashboard` | Tenure IQ \| Property Intelligence Platform | Specific dashboard title + value-prop description |
| `/properties-v2` | (default) | "Properties — Tenure IQ" + 1-line desc |
| `/properties-v2/:id` | (default) | Dynamic per-property title (e.g. "{address} — Tenure IQ") |
| `/tenants-v2` | (default) | Title + desc |
| `/tenants-v2/:id` | (default) | Dynamic per-tenant title |
| `/compliance/calendar` | (default) | Title + desc |
| `/compliance` (V2) | (default) | Title + desc |
| `/reports`, `/reports/*` | (default) | Title + desc |
| `/tax-dashboard` | (default) | Title + desc |
| `/onboarding-welcome` | (default) | Title + desc |
| `/signup`, `/login` (`/auth`) | (default) | Title + desc — note `/auth` is in robots `Disallow` so SEO copy is lower priority |
| `/jobs-and-works`, `/rent`, `/insurance`, `/capex`, `/lending/*`, `/investors`, `/entities`, `/documents`, `/settings`, `/team`, `/notifications`, `/inbox`, `/chat`, etc. | (default) | All inherit defaults — need David's voice |

## Uncertain — assets
- `/og-image.jpg` exists (24 KB). Confirm it renders well at Twitter/LinkedIn preview (1200×630). If Tenure IQ branding has refreshed since this asset was uploaded, replace it.
- `aggregateRating` (4.8 / 27) in JSON-LD is a placeholder — confirm or remove before launch (Google penalises fabricated ratings).
- `priceValidUntil: 2027-01-01` — keep current or move to a rolling date.
